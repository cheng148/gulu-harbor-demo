# 产品决策记录

## 2026-08-09：候选宠物档案来源与平台角色

- 状态：用户已确认。
- 咕噜港从合作商家提供的候选宠物中进行匹配，并帮助用户联系商家进一步了解；平台不直接销售宠物。
- “候选宠物档案”是统一名称。当前Demo使用预置的模拟合作商家、模拟在售宠物和模拟联系入口；未来可以替换为真实合作商家数据。
- 当前Demo的候选宠物与商家必须预先存在于数据集中，DeepSeek和Mock不得在对话时临时生成。
- 页面必须提示“Demo模拟数据，不代表真实在售”。平台不对宠物健康、性格或持续在售作绝对保证。
- 尚待确认：合作协议、资料核验等级、在售状态更新时间和违约处理。

## 2026-08-09：第一版客户端形态

- 状态：用户已确认。
- 第一版采用移动端优先的Web App/PWA，主要通过链接分享给用户体验；在支持的手机浏览器中可以添加到桌面。
- 第一版不是微信／支付宝小程序，也不描述为已经上架应用商店的原生Android或iPhone App。
- 移动端是主要验收场景，桌面端继续保持核心流程可用。
- 正式Android和iPhone App在第一版Agent体验得到验证后再决定；应用商店上架、原生推送和完整离线能力不属于第一版承诺。


## 2026-08-09：LangGraph只负责显式流程编排

- 状态：Gate 1已批准，Phase 3的T18至T22已验证完成。
- 采用LangGraph `StateGraph`展示节点和条件跳转，首个锁定版本为1.2.10。
- 双层推荐先独立判断方向，再在入选方向内选择具体宠物；单只宠物不得代表整个方向，方向合适但无合适个体时允许只展示方向。
- 不采用LangChain高层Agent或自由工具循环；模型不能决定硬约束、问题选择、评分、等级或排序。
- SQLite会话仓储仍是唯一状态事实来源；LangGraph不使用checkpointer另存一套会话真相。
- 模型输出必须先通过严格Schema和消息来源校验，之后才允许在后续任务中进入画像合并。
## 2026-08-10：核心手机页面先原型审批再开发

- 状态：用户已确认计划顺序。
- T23公共错误格式和配置启动检查已获批准、实现并验证；项目现暂停在UX-01原型审批前。
- 在T24之前提交核心手机页面原型，至少覆盖首页、AI对话、画像摘要、推荐前过敏确认、双层推荐结果、修改条件，以及错误／过期／重置反馈。
- 用户逐页确认页面结构、功能位置和主要操作流程后，才允许开始T24及后续页面相关任务。
- 具体原型制作工具、外部平台使用和视觉素材来源仍需在开始原型任务前说明并确认；本决策不授权生产代码、DeepSeek调用或部署。
## 2026-08-10：T23公共错误与配置启动边界

- 状态：T23已按TDD完成并验证。
- 所有公共错误响应统一包含机器可读错误码、用户可理解信息、是否可重试、详情和请求ID；未分类异常不暴露服务器栈、内部路径或敏感配置。
- 健康检查只返回服务与配置可用状态，不返回模型提供方、模型名、Key、环境变量值或数据库路径。
- Mock模式无需且不会读取DeepSeek Key；DeepSeek模式缺少Key或模型提供方名称无效时，服务在启动阶段明确失败。
- 本任务没有接入DeepSeek、会话API或前端页面；下一步仍是UX-01用户审批门禁。
## 2026-08-11：UX-01原型与视觉选择

- 用户批准制作独立的本地可点击手机端原型；不连接真实Agent、SQLite、DeepSeek或交易后台。
- 视觉采用第一套“港湾手账”的换色前原版；后来三套换色方案不采用。
- 该决定不授权T24、生产页面开发、真实模型调用或部署；UX-A仍需逐页验收。

## 2026-08-11：AI选宠入口与发现式首页分离
- 选定的“港湾手账”画面是AI选宠入口页，底部导航高亮“选宠”，不是整个App首页。
- App首页采用类似淘宝的发现式结构：搜索、分类、AI选宠入口、推荐宠物和合作店铺。
- 该初版首页口径曾允许宠物卡展示模拟销量和模拟好评；2026-08-13已由下方“首页热度数据分层”决策取代。
- 演示排序不构成真实热门榜；未来接入真实商家数据时必须可追溯并替换模拟指标，禁止真假混用。
- 本决策只批准UX-01原型展示口径，不批准真实交易、商家后台、T24生产代码或部署。
# 2026-08-13 首页热度数据分层与原型底栏复用

- 具体宠物个体不再展示销量和好评率；四只宠物组成“近期人气榜”，展示近7天浏览与想进一步了解人数。
- 新增“本期热门品种”，展示品种层面的模拟合作商家成交数量、相关交易好评率和当前模拟在售数量。
- 合作店铺保留店铺层面的模拟成交与好评指标；三类数据不得混用。
- 首页顶部增加24小时本地保存提示与轻量海港装饰，搜索框使用放大镜图标。
- 首页底栏复用UX-01原型的房子、脚印、社区、人物四个固定图标和选中样式，状态切换不得改变图标或底栏位置。




## 2026-08-13：T28接口类型与浏览器会话边界

- 前端接口类型由后端提交到仓库的OpenAPI快照生成，锁定`openapi-typescript` 7.12.0；总验证使用检查模式阻止“后端接口已变化、前端类型未更新”的提交。
- 前端统一API客户端只处理公共会话接口和统一错误格式，不保存、不发送DeepSeek Key；Key只能留在后端环境变量中。
- 浏览器本地只保存`conversationId`和`expiresAt`。记录过期、格式损坏或出现额外字段时立即清除，完整画像和消息仍以后端SQLite为准。
- 生成的TypeScript类型提供开发期字段约束，不宣称能单独验证真实网络响应；运行时异常继续由公共错误契约与后续浏览器流程测试覆盖。

## 2026-08-14：T33 DeepSeek适配器边界

- 状态：T33已按TDD完成；这不是T35真人模型演示完成记录。
- 使用DeepSeek当前OpenAI兼容`/chat/completions`接口，默认模型为`deepseek-v4-flash`，模型名仍由`DEEPSEEK_MODEL`配置；接口地址必须使用HTTPS。
- 请求采用非流式JSON Output，提示中明确要求JSON并附输出Schema；显式关闭thinking，不处理或保存推理内容。默认超时30秒，可由`DEEPSEEK_TIMEOUT_SECONDS`调整。
- DeepSeek和Mock复用四类输入输出Schema及两项语义保护：画像增量只能指向本轮消息，模型不能更换确定性规则选中的问题或快捷选项。过滤、评分、等级和排序仍不交给模型。
- DeepSeek响应作为不可信外部数据处理：响应外壳、结束原因、非空内容、JSON和业务Schema逐层校验；错误信息不回显Key或提供方响应正文。
- T33自动化全部使用假HTTP响应，不访问DeepSeek、不产生费用。无效响应后的最多一次受控重试、事务回滚和TTL保护属于T34，不在T33中提前实现。
- 官方依据：[DeepSeek Chat Completion](https://api-docs.deepseek.com/api/create-chat-completion)、[JSON Output](https://api-docs.deepseek.com/guides/json_mode/)、[Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode)、[HTTPX超时](https://www.python-httpx.org/advanced/timeouts/)、[HTTPX MockTransport](https://www.python-httpx.org/advanced/transports/)。

## 2026-08-14：T34受控重试与失败回滚边界

- 状态：T34已按TDD完成；这不是T35真人模型演示完成记录。
- 空内容、截断、无效JSON、错误响应外壳或错误业务Schema统一视为`INVALID_OUTPUT`，对同一次模型操作最多立即重试一次，即最多发出两次相同请求。
- 超时、网络错误、HTTP错误和鉴权失败不在适配器内自动重试。原因是这类失败无法可靠判断提供方是否已经处理请求，自动重复可能增加延迟、重复调用和费用；公共接口仍返回可重试错误，由用户决定是否重试整轮。
- 模型处理发生在会话写入之前。只有本轮全部成功后才一次性写入SQLite；两次无效响应或超时不会提交revision、消息、画像、推荐、TTL或成功幂等记录。
- 自动化使用`httpx.MockTransport`和本地SQLite验证，不读取真实Key、不访问DeepSeek、不产生费用。
