# PROJECT_CONTEXT.md

## 1. 项目概览

- 项目名称：咕噜港 - AI选宠顾问
- 项目形态：完整宠物服务平台前端原型 + 真实多轮选宠Agent Demo
- 核心目标：使用SDD、TDD和Agent工程方法，把养宠新手的模糊需求转化为可解释、可验证的选宠建议
- 核心用户：缺少养宠经验、难以一次说清自身条件的新手
- 当前阶段：T43展示材料整理已完成；下一项为T44最终全量验证与用户验收
- 当前Gate 0规格：`specs/SDD_SPEC.md`
- 历史资料：`references/咕噜港_原始项目策划书.pdf`

## 2. 事实来源与历史资料边界

本项目的事实优先级为：

1. 用户在当前对话中的最新确认
2. 已获用户批准的`specs/SDD_SPEC.md`
3. `PROJECT_CONTEXT.md`
4. 原项目策划书

原项目策划书只用于说明历史背景、曾识别的用户痛点和大概功能范围，不约束当前技术实现、商业模式、交易流程、算法、页面结构或验收口径。

历史资料仍有参考价值的判断包括：新手对品种和养护要求认知不足，难以准确表达需求；原“智能选宠”要求用户一次输入种类、品种、毛色、性别、年龄和价格等偏好，本质上更接近筛选与排序；原计划中的交易、会员、社区和用品可作为平台前端外壳的功能参考。

历史资料中关于真实交易、担保、合同、物流、AI医生、商业预测、旧技术栈和旧推荐算法的描述，不属于当前Demo承诺。

## 3. 核心产品判断

新手的问题不是缺少一个更长的筛选器，而是：

- 不知道哪些生活条件会决定自己是否适合养宠。
- 不知道应该先表达硬约束还是外观偏好。
- 容易把品种倾向误解成个体保证。
- 容易忽略时间、预算、家庭安全和长期责任。

因此，当前唯一做深的真实能力是“AI选宠顾问”：通过多轮对话逐步形成用户画像，优先识别硬约束和风险，根据当前状态动态选择下一问，再给出有证据、包含代价的建议。

一句话定位：

> 咕噜港AI选宠顾问通过多轮对话理解用户的生活方式、照护能力和真实偏好，再给出“品种/类型方向 + 候选宠物档案”的两层可解释建议；候选档案来自合作商家，咕噜港负责匹配和连接，不直接销售宠物。

它不是兽医、交易担保方、疾病诊断工具，也不是对品种个体性格或健康的保证工具。

## 4. 已确认的Gate 0决策

### 4.1 推荐采用两层结构

正式结果最多展示4个“品种/类型方向”，并从每个入选方向中选择1个候选宠物档案，最多展示4只：

- 第一层回答“什么类型更可能适合当前生活条件”。方向可以是品种，也可以是更稳妥的类型描述，例如“性格记录较完整的成年短毛家猫”，不强迫所有结论落到纯种品种。
- 第二层回答“合作商家的候选宠物中，哪些具体个体值得进一步了解”。当前Demo使用预先写入数据集的“模拟合作商家 + 模拟在售宠物”，不允许模型在对话时临时编造。
- 两层判断顺序已确认：先依据方向自身的群体倾向独立判断和排序，再在每个入选方向内选择最合适的具体候选宠物；不得用某只宠物的个体得分反向代替整个方向的判断。
- 方向合适但该方向没有通过过滤的具体候选时，允许只展示方向并明确暂无合适个体，不凑数。
- “候选宠物档案”是跨阶段总称：当前指模拟商家的模拟在售档案，未来接入后指真实合作商家提供的真实候选档案；两者使用同一业务结构，但必须清楚标明数据性质。
- 品种或类型只表达群体倾向；具体个体必须依据档案中的已知观察、照护需求和注意事项单独判断。
- 如果方向成立但当前档案库没有合适个体，可以只展示方向并明确“当前Demo暂无合适候选”，不得临时编造宠物。

### 4.2 页面不展示精确匹配分

- 数字分只用于内部排序、回归测试和评测。
- 用户页面和对外API只展示匹配等级、排序、理由、代价和不匹配点。
- 推荐结果使用“很合拍”“值得认识”“需要磨合”三个匹配等级；触发过敏或用户明确底线时使用“暂不合适”，且不进入正式推荐列表。
- 每个等级必须紧跟具体原因：“很合拍”说明合拍点；“值得认识”说明待确认信息或用户已接受的成立条件；“需要磨合”说明已确认且尚未解决的明显差距；“暂不合适”说明触发的过敏或明确底线。
- 等级文案不能包装成科学概率，也不能只给标签而不解释。
- 硬约束不通过的候选必须先被排除，不能通过软偏好高分抵消。

### 4.3 真人演示与自动化测试模型分离

- 真人演示使用DeepSeek API，具体模型名通过环境变量配置，并在实施前依据官方文档确认。
- 自动化测试必须使用确定性Mock模型：同一输入和测试场景产生稳定、可复现的结构化输出，不访问外网，不消耗真实API额度。
- Mock只替换语言模型提供方，不替换状态机、画像合并、冲突检测、追问策略、知识检索、硬约束过滤、排序、等级映射、安全检查或API Schema。
- DeepSeek与Mock必须遵守同一模型适配器输入输出契约和同一结构校验；真实模型输出无效时不得绕过校验。

### 4.4 无账户会话本地保存24小时

- 首版不建设账户系统。
- 会话状态保存在Demo本地持久化存储中，浏览器只持有不包含个人身份的会话标识和必要的界面状态。
- 每次成功的用户消息或画像修改后，会话过期时间延长为自该次活动起24小时。
- 24小时无成功活动后，会话失效；失效会话不能恢复或继续写入。
- 用户可以随时主动重置。重置后旧会话立即失效，前端创建全新会话，不继承旧画像或推荐。
- 不支持跨设备同步、账户找回或长期历史记录。

### 4.5 业务行为先规格、后TDD

- Gate 0批准前不编写生产代码。
- 实现细节可以在后续逐项探索，但每项新增或修改的业务行为必须先进入规格。
- 业务行为明确后，先写能够正确失败的测试，再写最小实现。
- 框架版本、存储实现、检索实现和部署方案可以后置探索，但不得用“实现待定”掩盖用户可观察行为、状态变化、错误语义或验收标准。
### 4.6 首版宠物方向与档案规模

- 首版只建立8个方向、16只模拟候选宠物，每个方向2只；每只关联一个模拟合作商家并标记为模拟在售，后续按评测和用户反馈逐步增加。
- 猫方向：成年本地／混种短毛猫、英国短毛猫、美国短毛猫、布偶猫。
- 狗方向：成年本地犬／混种犬（按个体档案匹配）、玩具型／迷你型贵宾犬、彭布罗克威尔士柯基犬、拉布拉多寻回犬。
- 成年本地／混种猫狗是“类型方向”，不冒充标准化纯种品种，也不承诺共同性格。
- 方向只描述有公开来源支持的群体倾向；体型、性格、互动、独处、叫声、掉毛和训练基础等个体差异，必须由具体档案覆盖或修正。
- 玩具型／迷你型贵宾犬的两只档案分别代表两种体型；“柯基犬”首版只指彭布罗克威尔士柯基犬，不与卡迪根混用。
- 首批不纳入法斗、巴哥等扁脸犬常规推荐；未来如加入，必须突出体型相关健康与福利风险，不得将热门度当作适合依据。
- 候选档案确认进度：16/16。8个方向的两只档案均已逐组确认：小麦、栗子、年糕、奶盖、花生、星星、糯米、蓝莓、阿福、豆包、可可、泡芙、土豆、团子、大麦、乌龙。

### 4.7 合作商家与候选档案定位

- 咕噜港不直接销售宠物，只根据用户情况匹配候选宠物，并帮助用户联系提供档案的合作商家进一步了解。
- 当前Demo没有真实商家接入：商家、宠物、在售状态和联系入口均为模拟数据，页面必须明确提示“Demo模拟数据，不代表真实在售”。
- 当前16只模拟候选宠物必须预先存在于数据集中，并关联模拟商家；DeepSeek和Mock都不能临时生成一只不存在的宠物。
- 未来真实产品可把同一结构的数据替换为合作商家提供的真实候选档案，但平台仍不对宠物健康、性格或持续在售作绝对保证。
- 合作协议、资料核验等级、在售状态更新时间和违约处理仍属待确认项，不能因本次定位确认而自行写死。

### 4.8 第一版客户端形态

- 第一版采用移动端优先的Web App/PWA，核心体验通过链接访问，并在支持的手机浏览器中允许用户添加到手机桌面。
- 第一版不是微信、支付宝等小程序，也不冒充已经上架应用商店的原生Android或iPhone App。
- 移动端是第一版主要体验和验收场景；现有桌面端可用性要求继续保留。
- 离线完整使用、应用商店上架、原生推送和其他原生系统能力不属于第一版承诺。
- 正式Android和iPhone App属于验证第一版Agent价值后的后续产品方向，具体技术和发布时间必须另行确认。

## 5. 项目范围与真实性分层

### 5.1 真实端到端能力

AI选宠Agent真实实现：

- 多轮会话与24小时本地状态
- 自由文本信息提取与显式画像槽位
- 已确认、推断、未知和冲突状态管理
- 基于缺失信息与风险的动态追问
- 硬约束、冲突和不适合养宠路径
- 可追溯的猫犬知识检索
- 品种/类型方向匹配
- 候选宠物档案过滤、排序与模拟商家关联
- 匹配等级、理由、代价、不匹配点和风险解释
- 条件修改、重新推荐和主动重置
- 确定性Mock测试、真人DeepSeek演示、评测集与Bad Case

### 5.2 可交互前端原型

- 首页
- 宠物市场/交易
- 宠物用品
- 社区
- 会员中心
- 我的

允许导航、搜索、筛选、排序、收藏、点赞、模拟购物车、弹窗、详情和本地状态反馈。

在页面相关接口和前端编码前，先完成一轮单独的核心手机页面原型审批。首轮至少覆盖首页、AI对话、画像摘要、推荐前过敏确认、双层推荐结果、修改条件，以及错误／过期／重置反馈。用户逐页确认页面结构、功能位置和主要操作流程后，才允许开始T24及其后续页面相关实现；原型制作工具与是否使用外部平台在开始原型任务前另行说明和确认。

2026-08-11，用户批准使用独立的本地可点击手机端原型推进UX-01，并最终选择“港湾手账”换色前原版作为视觉基准。2026-08-12，用户明确批准UX-01最终版并授权开始T24。原型仍只代表已确认的页面结构与交互基准；T24只实现创建和读取会话API，不接DeepSeek、不修改生产前端。

### 5.3 暂未开放

支付、真实下单、合同签署、物流、真实发帖、真实评论写入、会员付费、真实商家沟通、真实账户和真实订单统一提示；“联系商家进一步了解”在Demo中也只展示模拟入口：

> Demo演示功能，暂未开放。

不得以假接口或成功提示暗示真实业务后台已经存在。

## 6. Agent业务流程

```text
创建匿名会话
  -> 用户自由表达需求
  -> 校验输入并提取本轮画像增量
  -> 合并状态，保留来源、置信状态和用户确认
  -> 检查猫狗过敏、用户明确底线和画像冲突
  -> 若有阻断或冲突：优先解释并只追问一个关键问题
  -> 若关键信息不足：选择当前信息价值最高且未问过的问题
  -> 若信息足够或用户要求直接推荐：进入匹配
  -> 形成品种/类型方向
  -> 检索有来源的相关知识
  -> 对预置候选宠物档案执行硬过滤、内部评分和稳定排序，并保留模拟商家关联
  -> 映射为页面匹配等级并生成解释
  -> 安全复核
  -> 用户确认、修改条件、继续咨询或重置
```

## 7. 硬约束与风险分层

硬约束只来自两类已知事实：猫／狗过敏，或用户明确说出的绝对底线。它们不能被喜爱程度或其他高分抵消。

### 7.1 物种与候选排除

- 已确认对猫过敏：猫不进入正式推荐；已确认对狗过敏：狗不进入正式推荐。
- 用户明确说“绝对不能接受”的掉毛、叫声、体型、互动方式、来源等条件：违反该底线的方向或档案排除。
- 不得主动把住房许可、共同居住者、儿童、老人、孕妇、其他宠物或预算升级为产品必问项；用户主动提及时，才按其明确表达记录和处理。
- 若全部候选都违反已知底线，不强行凑数，也不评价用户是否“有资格养宠”；只说明当前没有合适选项，并允许用户决定是否修改条件。

### 7.2 差距而非排除

- 时间、陪伴、活动、清洁、美容等差距通常参与匹配等级判断，不自动排除。
- 若用户明确愿意为某一具体差距调整生活，该项按75%计入并附带成立条件；用户表达愿意即可，不要求证明具体改变计划。
- 愿意调整只处理用户明确接受的那一项，不能清除其他差距。

### 7.3 未知与冲突

- 未知项内部按该项一半权重计算，但状态仍是未知；只有可能影响当前结果的未知项才在页面标为“待确认”。
- 用户明确说“都可以／没有要求”属于已知中立，不是未知。
- 用户明确修正时以新值替换旧值；前后信息冲突且可能改变推荐时使用额外问题澄清，否则保留为待确认并给暂定结果。

## 8. 用户画像原则

画像围绕用户本人和宠物的相处关系，至少允许记录：

- 当前时间安排、宠物可能独处的情况和可投入的活动时间
- 期待的互动节奏与陪伴距离
- 对持续训练、活动、清洁和美容投入的接受度
- 对掉毛、叫声、气味等日常影响的接受度
- 猫狗范围、品种／类型与体型、年龄阶段、毛发／外观偏好
- 用户主动表达的绝对底线、愿意调整项和优先顺序
- 推荐前最后确认的猫狗过敏情况

住房、共同居住者、家庭成员、其他宠物和预算可以保留为用户主动披露的可选信息，但不是默认追问或正式推荐的必填条件。价格不主动询问；结果用获取成本等级和长期养护负担等级帮助用户自行比较。

每个关键槽位必须能区分：未知、模型推断、用户确认、用户拒答、发生冲突；并保留最近来源轮次。模型不得覆盖用户已经确认且未主动修改的信息。

## 9. 动态追问原则

- 采用“3+1”：通常最多3个核心动态问题；只有未知信息可能改变前两名候选、改变重要匹配等级或存在关键冲突时，才增加第4个针对性问题。
- 目标不是问完12题，而是从初始表达和问题库中收集足以稳定推荐的最少信息。
- 每轮默认只问一个问题；措辞温暖自然但不过度卖萌，快捷选项不能限制用户自由输入。
- 核心问题优先覆盖时间情况、期待的相处关系和不能接受的日常负担，顺序由当前已知与缺失信息动态决定。
- 已经确认的信息不重复询问；用户修改后允许围绕新冲突再次澄清。
- 猫狗过敏放在核心问题结束后、正式推荐之前轻量确认；已知则跳过。不得用“低敏品种”替代物种过敏检查。
- 用户要求“先推荐”时可以提前给暂定结果，只显示可能影响当前结果的待确认项。
- 第4问不因前两名只是交换顺序、或备选内部换位而触发。
- LLM负责理解回答和把已选问题说得自然；是否需要第4问、询问哪个槽位，由确定性规则决定。
- 连续两次无法理解输入时，提供改写、查看示例或重置选择。

## 10. 推荐与解释原则

推荐由确定性业务逻辑和LLM分工完成：

1. 确定性逻辑判断硬约束、过滤候选、计算内部数字分、稳定排序和映射匹配等级。
2. 知识检索提供品种/类型方向、照护要求和风险依据。
3. LLM负责理解自然语言、生成单个追问的自然表达，以及把结构化事实转成用户可读解释。
4. 安全复核阻止绝对保证、医疗越界、缺少来源和编造档案。

推荐结果至少包含：

- 用户画像摘要和待确认项
- 推荐状态：最终、暂定或暂不建议
- 最多4个品种/类型方向
- 最多4个候选宠物档案，通常每个入选方向1只；当前均关联模拟合作商家
- 每项的页面匹配等级
- 适合原因、需要接受的代价、不匹配点
- 每日照护／运动需求、获取成本等级和长期养护负担等级
- 主要风险与依据来源
- 修改条件、进一步了解或暂缓养宠等下一步

正式结果先按“很合拍 > 值得认识 > 需要磨合”排序，同等级内再按内部数字分排序；“暂不合适”不进入正式推荐。若全部正式候选都为“需要磨合”，页面不得称为“优先推荐”。不得展示内部数字分，不得使用“一定亲人”“绝对不掉毛”“不会生病”等承诺。

## 11. 数据、知识与隐私边界

- 候选宠物档案、合作商家、在售状态、商品、帖子、订单和用户状态全部使用明确标注的模拟Mock数据。
- 品种和养护知识使用公开可靠资料或重新编写内容，每条保留来源标识。
- 品种倾向、具体个体观察和商家提供信息分层存储和展示；当前Demo不得暗示模拟资料已经由真实商家或平台核验。
- 不使用原项目真实用户、商家、交易、财务或问卷明细。
- 不主动收集姓名、手机号、精确住址、身份证、支付信息或详细医疗记录。
- 对话仅用于当前无账户Demo会话；24小时过期或主动重置后不可继续使用。

## 12. 技术方向与未锁定实现

当前方向：

- 前端：移动端优先的Next.js Web App/PWA + TypeScript + Tailwind CSS
- Agent服务：Python + FastAPI + LangGraph显式状态图；LangGraph只负责编排
- 真人演示模型：DeepSeek API，提供方和模型名经环境变量配置
- 自动化测试模型：确定性Mock
- 状态：支持24小时过期和主动重置的本地轻量持久化
- 数据：本地猫犬知识、模拟合作商家与预置候选宠物档案
- 测试：前后端单元测试、Agent集成测试、API契约测试和端到端演示测试

以下属于Gate 0批准后的实现探索项，不改变本文件已定义的业务行为：

- LangGraph已采用并锁定为首个验证版本1.2.10；不采用LangChain高层Agent或自由工具循环
- JSON、SQLite或其他本地轻量存储的最终选择
- 小规模知识检索是否需要Embedding或向量库
- 前后端测试工具和依赖版本
- 部署平台、GitHub可见性和图片方案

所有依赖版本必须在实施前依据官方文档验证并锁定，不凭记忆写死。

## 13. 真实性表述

- 已设计：已写入规格，但尚未实现。
- 已编码：已有代码，但不代表可运行。
- 已运行：可在目标环境启动。
- 已测试：存在通过的自动化或人工验证证据。
- 已部署：已发布到用户批准的平台。

当前项目可以表述为“Gate 0修订版、Gate 1、UX-01最终版、T06R至T43以及T35B已完成，Phase 6与Phase 7均已通过”。2026-09-05，`deepseek-v4-flash`（官方版本`DeepSeek-V4-Flash-0731`）完成一条5轮真人流程；主API真实调用模型完成画像提取和问题措辞，确定性代码完成追问选择、过滤、评分与排序。最终通过轮次共11次模型调用、15,439 Token，费用估算约0.017元人民币。真人演示暴露的单值／数组差异、定性花费误入数字预算和非标准匹配标签均已通过TDD修复。T35B随后把推荐解释和安全复核接入主API：模型只能根据已锁定事实生成`aiNarrative`，不能改变候选、顺序或匹配等级；解释数量无效或复核无法给出完整安全替换时整轮回滚。该接线已由确定性Mock、API回滚测试和手机Chromium五轮流程验证，尚未再次消耗真实DeepSeek额度做接线后的真人复跑。前端已完成手机首页、类型安全API、本地会话控制、AI对话、相处画像、双层推荐结果、AI整理说明、条件修改、失败重试、过期提示、主动重置、宠物市场、宠物用品、社区、会员中心、“我的”和Web App Manifest。T41已完成固定Mock评测；T42已完成七页浏览器、320至430px手机宽度、1280px桌面、键盘、控制台、网络和PWA清单审查；T43已补齐并校对README、架构、决策、演示脚本与评测报告。T39及其可读性修订已发布到`https://gulu-harbor-demo.yuanchen863.chatgpt.site`；该地址是公开原型分享版，不等于完整FastAPI与DeepSeek后端已部署，T35B及T40至T43也尚未发布到该网址。Phase 8还需完成T44最终验收；实体手机系统安装仍需完整前后端拥有可访问HTTPS地址后手工确认。

## 14. Gate 0批准与本轮修订记录

用户已于2026-08-04在当前对话中明确回复“批准 Gate 0”，完成基础Gate 0批准。2026-08-09，用户又对动态问题、判断规则、目录、平台角色和16只模拟候选宠物档案逐项确认，并明确回复“批准 Gate 0 修订版”。当前已批准的修订版覆盖：

- 历史策划书仅为参考，不约束当前实现。
- 真实核心是多轮选宠Agent，其他模块是可点击原型。
- 推荐采用“品种/类型方向 + 候选宠物档案”两层结构；当前候选档案来自模拟合作商家，未来可替换为真实合作商家数据。
- 页面和对外API不展示内部数字分。
- DeepSeek只用于真人演示；自动化测试使用确定性Mock，且Mock不替换业务逻辑。
- 猫狗过敏和用户明确底线会在评分前排除对应候选，不能被偏好高分抵消。
- 动态追问规则、状态变化和直接推荐降级路径可测试。
- 无账户会话按最近成功活动保存24小时，并支持立即重置。
- Agent状态、API输入输出、错误语义和验收用例已在SDD中明确。
- 第一版采用移动端优先的Web App/PWA，可通过链接体验并在支持的手机浏览器中添加到桌面；不做小程序，不宣称原生App已上架。

Gate 0修订版、Gate 1、第一批Phase 0与第二批Phase 1任务均已获批准；第一版客户端形态补充决策也已确认。规格批准只锁定产品行为，不自动授权修改生产代码、实施计划或任务清单；后续动作仍需按当前门禁单独确认。

2026-08-10，用户确认在T23之后、T24之前新增“核心手机页面原型审批”门禁。该确认只授权更新规格、计划、任务清单和决策记录，不授权T23、原型制作或任何生产代码。原型批准前不得开始T24及其后续页面相关任务。

## 15. Gate 1批准记录

用户已于2026-08-04在当前对话中明确回复“同意这个顺序（我有deepseek的api）”。这表示：

- 批准`specs/IMPLEMENTATION_PLAN.md`中的Phase 0至Phase 8顺序、技术边界和检查点。
- 允许生成`specs/TASKS.md`，但不等于允许立刻编码。
- 用户已确认具备DeepSeek API访问权限；项目不在聊天、文档或代码仓库中收集真实Key，接入时仅由用户放入本机`.env`。
- 下一道门禁是批准第一批任务；批准后才按RED、GREEN、REFACTOR顺序实施。

## 16. Gate 0之后仍需确认的事项

以下事项不影响已经批准的基础Gate 0，但在实施对应能力前必须确认或记录决策：

- 未来合作商家协议、资料核验等级、在售状态更新时间与违约处理

- 猫犬知识的具体来源清单和引用格式
- 依赖版本与模型名
- 本地持久化和知识检索的具体实现
- 图片采用授权图库、AI生成还是统一插画
- 部署平台与GitHub仓库可见性
2026-08-11，用户进一步确认选定的“港湾手账”画面属于“AI选宠入口页”，不是整个App首页；底部导航应高亮“选宠”。整个App首页采用类似淘宝的发现式信息流，包含搜索、猫狗／店铺分类、AI选宠入口、推荐宠物和合作店铺。

2026-08-13，用户进一步明确：第一版采用Web App只是为了节约开发时间和成本，最终产品目标仍是手机App。因此生产前端在手机上铺满屏幕，在电脑预览时固定为430px手机App宽度并居中，不随宽屏扩展成普通网页；推荐宠物保持两列，底栏与手机容器等宽。此展示规则已在T27修订中实现并验证。

2026-08-13，用户修正首页数据口径并批准新版结构：具体宠物是独一无二的个体，不能展示销量或好评率；原“本期推荐”改为“近期人气榜”，四只宠物保持不变，只展示近7天浏览次数和想进一步了解人数等个体关注指标。首页另增“本期热门品种”，展示品种层面的模拟合作商家汇总数据，包括近30天成交数量、相关交易好评率和当前模拟在售数量。合作店铺可以展示店铺层面的模拟成交与好评指标。页面级与每张相关卡片仍须标明“Demo虚拟数据”；真实与模拟指标不得混用，个体关注度、品种交易汇总和店铺交易指标不得互相冒充。首页顶部增加“24小时本地保存”提示和轻量海港装饰；搜索框使用放大镜图标；底部导航沿用已批准原型的房子、脚印、社区、人物图标与选中样式，切换状态不得改变图标或底栏位置。

2026-08-14，用户在手机复查T37分享版后批准五项修订：分享版首页恢复“本期热门品种”；宠物市场不再把全部个体平铺在入口页，而按“品种/类型方向 → 具体模拟在售宠物 → 所在模拟合作商家”逐层浏览；首版保留布偶猫、英国短毛猫、柯基犬、拉布拉多犬四个品种，每个品种展示两只模拟个体。用品购物车数量为0时不显示；加入后在底部主导航上方固定显示，并明确反馈“已加入模拟购物车，本机共有 N 件用品”。首次打开分享网址默认进入“首页”并高亮首页，用户点击“选宠”后才进入AI选宠入口。以上修订必须同时进入主前端和同网址手机分享版，不能只改其中一个版本。
