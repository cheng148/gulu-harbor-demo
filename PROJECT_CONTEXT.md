# PROJECT_CONTEXT.md

## 1. 项目概览

- 项目名称：咕噜港 - AI选宠顾问
- 项目形态：完整宠物服务平台前端原型 + 真实多轮选宠Agent Demo
- 核心目标：展示AI产品经理使用SDD、TDD和Agent工程方法，把模糊的新手选宠需求转化为可解释、可验证的建议
- 核心用户：缺少养宠经验、难以一次说清自身条件的新手
- 当前阶段：Phase 0工具链与契约地基已完成并验证；第二批Phase 1任务待用户批准
- 当前Gate 0规格：`specs/SDD_SPEC.md`
- 历史资料：`references/咕噜港_原始项目策划书.pdf`

## 2. 事实来源与历史资料边界

本项目的事实优先级为：

1. 用户在当前对话中的最新确认
2. 已获用户批准的`specs/SDD_SPEC.md`
3. `PROJECT_CONTEXT.md`
4. 原项目策划书

原项目策划书只用于说明历史背景、曾识别的用户痛点和大概功能范围，不约束当前技术实现、商业模式、交易流程、算法、页面结构或验收口径。

历史资料仍有参考价值的判断包括：新手对品种和养护要求认知不足，难以准确表达需求；原“智能选宠”要求用户一次输入种类、品种、毛色、性别、年龄和价格等偏好，本质上更接近筛选与排序；原计划中的交易、会员、社区和用品可作为平台前端外壳的功能参考。

历史资料中关于真实交易、担保、合同、物流、AI医生、商业预测、旧技术栈和旧推荐算法的描述，不属于当前Demo承诺。

## 3. 核心产品判断

新手的问题不是缺少一个更长的筛选器，而是：

- 不知道哪些生活条件会决定自己是否适合养宠。
- 不知道应该先表达硬约束还是外观偏好。
- 容易把品种倾向误解成个体保证。
- 容易忽略时间、预算、家庭安全和长期责任。

因此，当前唯一做深的真实能力是“AI选宠顾问”：通过多轮对话逐步形成用户画像，优先识别硬约束和风险，根据当前状态动态选择下一问，再给出有证据、包含代价的建议。

一句话定位：

> 咕噜港AI选宠顾问通过多轮对话理解用户的生活方式、照护能力和真实偏好，先判断是否适合进入选宠，再给出“品种/类型方向 + 具体虚构宠物档案”的两层可解释建议。

它不是兽医、交易担保方、疾病诊断工具，也不是对品种个体性格或健康的保证工具。

## 4. 已确认的Gate 0决策

### 4.1 推荐采用两层结构

推荐先给出1至3个“品种/类型方向”，再给出与方向关联的0至3个具体虚构宠物档案：

- 第一层回答“什么类型更可能适合当前生活条件”。方向可以是品种，也可以是更稳妥的类型描述，例如“性格记录较完整的成年短毛家猫”，不强迫所有结论落到纯种品种。
- 第二层回答“Demo档案中有哪些具体个体值得进一步了解”。具体档案必须是数据集中真实存在的虚构记录，不允许模型临时编造。
- 品种或类型只表达群体倾向；具体个体必须依据档案中的已知观察、照护需求和注意事项单独判断。
- 如果方向成立但当前档案库没有合适个体，可以只展示方向并明确“当前Demo档案暂无合适个体”，不得为凑数降低硬约束。

### 4.2 页面不展示精确匹配分

- 数字分只用于内部排序、回归测试和评测。
- 用户页面和对外API只展示匹配等级、排序、理由、代价和不匹配点。
- 页面匹配等级为“较匹配”“可考虑”“需谨慎”，不能包装成科学概率。
- 硬约束不通过的候选必须先被排除，不能通过软偏好高分抵消。

### 4.3 真人演示与自动化测试模型分离

- 真人演示使用DeepSeek API，具体模型名通过环境变量配置，并在实施前依据官方文档确认。
- 自动化测试必须使用确定性Mock模型：同一输入和测试场景产生稳定、可复现的结构化输出，不访问外网，不消耗真实API额度。
- Mock只替换语言模型提供方，不替换状态机、画像合并、冲突检测、追问策略、知识检索、硬约束过滤、排序、等级映射、安全检查或API Schema。
- DeepSeek与Mock必须遵守同一模型适配器输入输出契约和同一结构校验；真实模型输出无效时不得绕过校验。

### 4.4 无账户会话本地保存24小时

- 首版不建设账户系统。
- 会话状态保存在Demo本地持久化存储中，浏览器只持有不包含个人身份的会话标识和必要的界面状态。
- 每次成功的用户消息或画像修改后，会话过期时间延长为自该次活动起24小时。
- 24小时无成功活动后，会话失效；失效会话不能恢复或继续写入。
- 用户可以随时主动重置。重置后旧会话立即失效，前端创建全新会话，不继承旧画像或推荐。
- 不支持跨设备同步、账户找回或长期历史记录。

### 4.5 业务行为先规格、后TDD

- Gate 0批准前不编写生产代码。
- 实现细节可以在后续逐项探索，但每项新增或修改的业务行为必须先进入规格。
- 业务行为明确后，先写能够正确失败的测试，再写最小实现。
- 框架版本、存储实现、检索实现和部署方案可以后置探索，但不得用“实现待定”掩盖用户可观察行为、状态变化、错误语义或验收标准。

## 5. 项目范围与真实性分层

### 5.1 真实端到端能力

AI选宠Agent真实实现：

- 多轮会话与24小时本地状态
- 自由文本信息提取与显式画像槽位
- 已确认、推断、未知和冲突状态管理
- 基于缺失信息与风险的动态追问
- 硬约束、冲突和不适合养宠路径
- 可追溯的猫犬知识检索
- 品种/类型方向匹配
- 具体虚构宠物档案过滤与排序
- 匹配等级、理由、代价、不匹配点和风险解释
- 条件修改、重新推荐和主动重置
- 确定性Mock测试、真人DeepSeek演示、评测集与Bad Case

### 5.2 可交互前端原型

- 首页
- 宠物市场/交易
- 宠物用品
- 社区
- 会员中心
- 我的

允许导航、搜索、筛选、排序、收藏、点赞、模拟购物车、弹窗、详情和本地状态反馈。

### 5.3 暂未开放

支付、真实下单、合同签署、物流、真实发帖、真实评论写入、会员付费、真实商家沟通、真实账户和真实订单统一提示：

> Demo演示功能，暂未开放。

不得以假接口或成功提示暗示真实业务后台已经存在。

## 6. Agent业务流程

```text
创建匿名会话
  -> 用户自由表达需求
  -> 校验输入并提取本轮画像增量
  -> 合并状态，保留来源、置信状态和用户确认
  -> 检查会话级阻断风险、候选级硬约束和画像冲突
  -> 若有阻断或冲突：优先解释并只追问一个关键问题
  -> 若关键信息不足：选择当前信息价值最高且未问过的问题
  -> 若信息足够或用户要求直接推荐：进入匹配
  -> 形成品种/类型方向
  -> 检索有来源的相关知识
  -> 对虚构宠物档案执行硬过滤、内部评分和稳定排序
  -> 映射为页面匹配等级并生成解释
  -> 安全复核
  -> 用户确认、修改条件、继续咨询或重置
```

## 7. 硬约束与风险分层

硬约束是违反后不能靠外观或其他软偏好补偿的条件。系统必须区分三类结果：

### 7.1 会话级阻断

在以下情况未解决前，不输出“最终推荐”：

- 明确不允许养宠，或关键共同居住者明确反对。
- 严重过敏或已知高健康风险尚未获得专业建议；系统不得声称存在“绝对不过敏”的品种。
- 没有任何稳定照护安排。
- 用户明确无法承担最低基础照护成本，且没有可行调整方案。
- 涉及儿童、老人、孕妇或其他宠物的明显安全冲突尚未澄清。

系统可以输出“暂不建议养宠”“建议先解决前置条件”或非购买型替代行动，不得强行凑候选。

### 7.2 候选级排除

候选的最低需求与用户已确认的不可妥协条件冲突时，必须排除，例如：

- 运动、陪伴、空间或美容需求超过用户可稳定提供的上限。
- 月度基础成本下限超过用户硬预算上限。
- 档案明确不适合当前儿童年龄、其他宠物或居住环境。
- 掉毛、叫声、体型、来源等被用户明确标记为不可接受。

### 7.3 必须澄清

信息为未知、用户表达含糊、两个已确认条件互相冲突，或只有模型推断没有用户确认时，应先追问，不得把推测当事实。

紧急医疗预算未准备、部分软偏好未知等情况可以产生暂定建议，但必须显示待确认项和风险。

## 8. 用户画像原则

画像至少覆盖：

- 可养性：住房类型、宠物许可、共同居住者同意情况
- 家庭安全：儿童、老人、孕妇、其他宠物及相关约束
- 健康风险：过敏情况和用户主动披露的相关限制
- 照护能力：稳定陪伴时间、运动时间、独处安排、经验水平
- 经济能力：月度基础预算、一次性准备、突发医疗准备
- 环境能力：空间、户外条件、噪音限制
- 负担接受度：掉毛、气味、叫声、清洁和美容
- 软偏好：猫狗、体型、年龄、活跃度、亲人程度、外观、领养或购买

每个关键槽位必须能区分：未知、模型推断、用户确认、用户拒答、发生冲突；并保留最近来源轮次。模型不得覆盖用户已经确认且未主动修改的信息。

## 9. 动态追问原则

- 每轮默认只问一个问题；快捷选项不能限制用户自由输入。
- 优先级为：会话级阻断风险 > 当前冲突 > 缺失的关键硬约束 > 能显著改变候选集合的问题 > 软偏好。
- 已经确认的信息不重复询问；用户修改后允许围绕新冲突再次澄清。
- 同等优先级下使用稳定顺序，确保Mock测试可复现。
- 默认目标是在5至8轮形成初步建议，但完整性和安全性高于轮数。
- 用户要求“直接推荐”时可以跳过非阻断信息，结果必须标为暂定并列出待确认项；会话级阻断不能被跳过。
- 连续两次无法理解输入时，提供改写、查看示例或重置选择。

## 10. 推荐与解释原则

推荐由确定性业务逻辑和LLM分工完成：

1. 确定性逻辑判断硬约束、过滤候选、计算内部数字分、稳定排序和映射匹配等级。
2. 知识检索提供品种/类型方向、照护要求和风险依据。
3. LLM负责理解自然语言、生成单个追问的自然表达，以及把结构化事实转成用户可读解释。
4. 安全复核阻止绝对保证、医疗越界、缺少来源和编造档案。

推荐结果至少包含：

- 用户画像摘要和待确认项
- 推荐状态：最终、暂定或暂不建议
- 1至3个品种/类型方向
- 0至3个关联的具体虚构宠物档案
- 每项的页面匹配等级
- 适合原因、需要接受的代价、不匹配点
- 每日照护/运动需求和月度基础成本区间
- 主要风险与依据来源
- 修改条件、进一步了解或暂缓养宠等下一步

不得展示内部数字分，不得使用“一定亲人”“绝对不掉毛”“不会生病”等承诺。

## 11. 数据、知识与隐私边界

- 宠物档案、商品、帖子、订单和用户状态全部使用虚构Mock数据。
- 品种和养护知识使用公开可靠资料或重新编写内容，每条保留来源标识。
- 品种倾向与具体个体事实分层存储和展示。
- 不使用原项目真实用户、商家、交易、财务或问卷明细。
- 不主动收集姓名、手机号、精确住址、身份证、支付信息或详细医疗记录。
- 对话仅用于当前无账户Demo会话；24小时过期或主动重置后不可继续使用。

## 12. 技术方向与未锁定实现

当前方向：

- 前端：Next.js App Router + TypeScript + Tailwind CSS
- Agent服务：Python + FastAPI + 显式状态图
- 真人演示模型：DeepSeek API，提供方和模型名经环境变量配置
- 自动化测试模型：确定性Mock
- 状态：支持24小时过期和主动重置的本地轻量持久化
- 数据：本地猫犬知识与虚构宠物档案
- 测试：前后端单元测试、Agent集成测试、API契约测试和端到端演示测试

以下属于Gate 0批准后的实现探索项，不改变本文件已定义的业务行为：

- 是否采用LangGraph/LangChain及具体版本
- JSON、SQLite或其他本地轻量存储的最终选择
- 小规模知识检索是否需要Embedding或向量库
- 前后端测试工具和依赖版本
- 部署平台、GitHub可见性和图片方案

所有依赖版本必须在实施前依据官方文档验证并锁定，不凭记忆写死。

## 13. 真实性表述

- 已设计：已写入规格，但尚未实现。
- 已编码：已有代码，但不代表可运行。
- 已运行：可在目标环境启动。
- 已测试：存在通过的自动化或人工验证证据。
- 已部署：已发布到用户批准的平台。

当前项目只能表述为“Gate 0规格与Gate 1实施计划已批准，任务清单待批准”，不能表述为Agent已编码、运行、测试、部署或Demo已完成。

## 14. Gate 0批准记录

用户已于2026-08-04在当前对话中明确回复“批准 Gate 0”。批准范围包括：

- 历史策划书仅为参考，不约束当前实现。
- 真实核心是多轮选宠Agent，其他模块是可点击原型。
- 推荐采用“品种/类型方向 + 具体虚构宠物档案”两层结构。
- 页面和对外API不展示内部数字分。
- DeepSeek只用于真人演示；自动化测试使用确定性Mock，且Mock不替换业务逻辑。
- 硬约束能导致澄清、候选排除或暂不建议，而不是被软分抵消。
- 动态追问规则、状态变化和直接推荐降级路径可测试。
- 无账户会话按最近成功活动保存24小时，并支持立即重置。
- Agent状态、API输入输出、错误语义和验收用例已在SDD中明确。

Gate 0现已通过，Gate 1实施计划已于2026-08-04获批，`specs/TASKS.md`已经生成。第一批任务未获批准前仍不得编写生产代码。

## 15. Gate 1批准记录

用户已于2026-08-04在当前对话中明确回复“同意这个顺序（我有deepseek的api）”。这表示：

- 批准`specs/IMPLEMENTATION_PLAN.md`中的Phase 0至Phase 8顺序、技术边界和检查点。
- 允许生成`specs/TASKS.md`，但不等于允许立刻编码。
- 用户已确认具备DeepSeek API访问权限；项目不在聊天、文档或代码仓库中收集真实Key，接入时仅由用户放入本机`.env`。
- 下一道门禁是批准第一批任务；批准后才按RED、GREEN、REFACTOR顺序实施。

## 16. Gate 0之后仍需确认的事项

以下事项不影响当前业务规格批准，但在实施对应能力前必须确认或记录决策：

- 猫犬知识的具体来源清单和引用格式
- 依赖版本与模型名
- 本地持久化和知识检索的具体实现
- 图片采用授权图库、AI生成还是统一插画
- 部署平台与GitHub仓库可见性